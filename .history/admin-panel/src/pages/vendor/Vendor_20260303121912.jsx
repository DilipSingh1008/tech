import React from 'react'
import { getItems, getItemById, createItem, updateItem } from '../../services/api'

const Vendor = () => {
  return (
    <div>
      
    </div>
  )
}

export default Vendor
